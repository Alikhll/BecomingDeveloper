namespace Samples;

// we can comment multiple filese with /* to start and */ to end
// I have commented because there is a bug in each function respectively
// therefore, you need to un comment (removing /* and */ for each part and run the project to see issues)
/*
public partial class TroubleShootingSample
{
    public static void Trouble1()
    {
        int a = 2
        // After running the project you will get the bellow error message
        // Trouble1.cs(11,18): error CS1002: ; expected
        // it has explained that in file "Trouble1.cs" at line "11" expects ";" which is missing!
        // you can add ; at the end and run the project again, should fix the issue
        // this is the correct one
        // int a = 2;
        Console.WriteLine(a);
    }
}
*/