namespace Methods;
/*
public partial class TroubleShootingSample
{
    public static void Trouble1()
    {
        // this is what .NET compiler complains about
        // error CS0815: Cannot assign void to an implicitly-typed variable
        // the 'Print' return type is void so that you are not allowed to retrieve from it
        // so it should be 'Print();' instead
        var d = Print();

        Console.WriteLine(d);
    }


    // in C# if you don't provide the access type, the default is private!
    static void Print()
    {
        Console.WriteLine("print!");
    }
}
*/