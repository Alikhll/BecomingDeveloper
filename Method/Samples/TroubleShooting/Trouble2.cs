namespace Methods;
/*
public partial class TroubleShootingSample
{
    public static void Trouble2()
    {
        // here parameter is not passed so it'll break 
        PrintName();
        // here we pass 'int' instead of 'string' so it breaks
        PrintName(123);
        // here we pass 'string' and another addtional param that doesn't fit the signiture so it breaks
        PrintName("name", bool);
    }


    static void PrintName(string name)
    {
        Console.WriteLine("Name: " + name);
    }
}
*/